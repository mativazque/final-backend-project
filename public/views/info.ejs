<div class="container mt-2">
    <div class="jumbotron">
        <h1 class="mb-3">Información de su cuenta</h1>
        <ol class="list-group list-group-numbered">
            <% data.forEach(item=> { %>
                <li class="list-group-item d-flex justify-content-between align-items-start">
                    <div class="ms-2 me-auto">
                        <h6 class="fw-bold">
                            <%=item.name%>
                        </h6>
                        <p class="m-0">
                            <%=item.value%>
                        </p>
                    </div>
                </li>

                <% }) %>
        </ol>
    </div>
</div>